{% extends "layout.njk" %}

{% block content %}

  <div id="admin-users">   
    <div class="header user">
      <p>Usuários</p>
      <a id="button-create" href="/admin/users/register">Novo</a>
    </div>
    <div class="container_cards_users">
      {% for userComun in usersComuns %} 
        <div class="users">
             <p id="user-name">{{userComun.name}}</p>
             <p id="user-email">{{userComun.email}}</p>
          <form class="link-user">
            <a id="button-user-edit" href="/admin/profile/{{userComun.id}}/edit">Editar</a>
          {# <button type="submit" id="button-user-edit" method="post" action="/admin/profile/{{userComun.id}}/edit">Editar</button> #}
          </form>
          {% if userComun.is_admin == false %} 
          <div class="link-user">
          <button id="button-user-delete" type="submit" method="post" action="/admin/users/delete?_method=DELETE">Deletar</a>      
          </div>
          {% endif %}
        </div>
      {% endfor %}
    </div>  
  </div>
  </div>
{% endblock %}