{% extends "layout.njk" %}

{% block content %}

  <div id="admin-users">
    
    <div class="header user">
      <p>Usuários</p>
      <a id="button-create" href="/admin/users/register">Novo</a>
    </div>
    <div class="container_cards_users">
      {% for user in users %}
  
        <div class="users">
             <p id="user-name">{{user.name}}</p>
             <p id="user-email">{{user.email}}</p>
          <form method="post" action="/admin/profile?_method=PUT">
          <button class="" type="submit">Editar</button>      
          <input type="hidden" name="id" value="{{user.id}}">
          </form>
          <form method="post" action="/admin/profile?_method=DELETE">
          <button class="" type="submit">Deletar</button>      
          <input type="hidden" name="id" value="{{user.id}}">
          </form>
        </div>
      {% endfor %}
    </div>
   
  </div>


  </div>

{% endblock %}